# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# SerialAgent — Environment Variables
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Copy this file to .env and fill in the values for your deployment.
# Variables with defaults are shown commented out.

# ── Gateway Core ─────────────────────────────────────────────────────

# Path to config.toml (default: config.toml in working directory)
# SA_CONFIG=config.toml

# Tracing filter for structured JSON logs (default: info,sa_gateway=debug)
# Uses tracing-subscriber EnvFilter syntax.
# RUST_LOG=info,sa_gateway=debug

# Max concurrent HTTP requests before backpressure (default: 256)
# SA_MAX_CONCURRENT_REQUESTS=256

# ── Authentication ───────────────────────────────────────────────────

# Bearer token for API endpoints. If set, all protected routes require
# Authorization: Bearer <token>. If unset, API auth is DISABLED (dev mode).
SA_API_TOKEN=

# Bearer token for admin endpoints (ClawHub install/update, import,
# system info, workspace files). If unset, admin endpoints return 403
# for ClawHub operations and allow unauthenticated access for others.
SA_ADMIN_TOKEN=

# ── LLM Provider API Keys ───────────────────────────────────────────
# Set the keys for providers enabled in config.toml [[llm.providers]].
# Only providers with valid keys will initialize; others are skipped.

# OpenAI (gpt-4o, gpt-4o-mini, text-embedding-3-small, etc.)
OPENAI_API_KEY=

# Anthropic (claude-sonnet-4-20250514, etc.)
ANTHROPIC_API_KEY=

# Google Gemini (gemini-2.0-flash, etc.)
GOOGLE_API_KEY=

# OpenRouter (unified gateway for many models)
# OPENROUTER_API_KEY=

# Together AI (open-source models)
# TOGETHER_API_KEY=

# Chutes (DeepSeek, etc.)
# CHUTES_API_KEY=

# xAI / Grok
# XAI_API_KEY=

# Alibaba Qwen (DashScope)
# DASHSCOPE_API_KEY=

# MiniMax
# MINIMAX_API_KEY=

# Moonshot
# MOONSHOT_API_KEY=

# HuggingFace Inference
# HF_TOKEN=

# Venice AI
# VENICE_API_KEY=

# LiteLLM proxy
# LITELLM_API_KEY=

# Cloudflare Workers AI
# CLOUDFLARE_API_KEY=

# Abort startup if no LLM providers initialize (default: false).
# Set to 1 for production when LLM is required.
# SA_REQUIRE_LLM=0

# ── Node Authentication ─────────────────────────────────────────────
# Nodes connect via WebSocket at /v1/nodes/ws.

# Global token for all node connections. If unset, node WS is open.
# SA_NODE_TOKEN=

# Per-node tokens (takes priority over SA_NODE_TOKEN).
# Format: node1:tokenA,node2:tokenB
# SA_NODE_TOKENS=mac1:secret1,pi:secret2

# Per-node capability allowlists. Nodes not listed are unrestricted.
# Format: nodeId:prefix1+prefix2,nodeId2:prefix3
# SA_NODE_CAPS=mac1:macos.notes+macos.calendar,pi:home.lights

# ── Node Client Env Vars ────────────────────────────────────────────
# These are used by sa-node binaries (hello-node, sa-node-macos), not
# the gateway itself.

# Node identity (auto-generated from hostname if unset)
# SA_NODE_ID=my-mac
# SA_NODE_NAME=My Mac Node
# SA_NODE_TAGS=macos,desktop

# Gateway WebSocket URL for node clients to connect to
# SA_GATEWAY_WS_URL=ws://localhost:3210/v1/nodes/ws

# Allowed directory for hello-node file operations
# SA_ALLOWED_DIR=.

# ── GitHub Integration ───────────────────────────────────────────────

# GitHub personal access token for ClawHub (private repos, rate limits).
# Optional — public repos work without it.
# GITHUB_TOKEN=

# ── Web Fetch Skill ──────────────────────────────────────────────────

# HTTP request timeout for web.fetch skill (default: 20 seconds)
# SA_WEB_TIMEOUT_SECS=20

# Max response body size in bytes (default: 5MB)
# SA_WEB_MAX_BYTES=5242880

# Max extracted text length in chars (default: 250000)
# SA_WEB_MAX_TEXT_CHARS=250000

# ── OpenClaw Import ──────────────────────────────────────────────────

# Max compressed tarball size in bytes (default: 200MB)
# SA_IMPORT_MAX_TGZ_BYTES=209715200

# Max total extracted size in bytes (default: 500MB)
# SA_IMPORT_MAX_EXTRACTED_BYTES=524288000

# Max number of files in archive (default: 50000)
# SA_IMPORT_MAX_FILE_COUNT=50000

# Allow SSH password auth for imports (default: false, prefer ssh-agent/keyfile)
# SA_IMPORT_ALLOW_SSH_PASSWORD=0

# ── Dashboard (Tauri) ───────────────────────────────────────────────

# Backend URL for the Tauri desktop app (default: http://localhost:3210)
# SA_BACKEND_URL=http://localhost:3210
